git_url(
  ENV["MATCH_GIT_URL"].to_s.strip.empty? ?
    "https://github.com/DamirMs/FlightLoopRush-Certs.git" :
    ENV["MATCH_GIT_URL"]
)

storage_mode("git")

app_id = ENV["BUNDLE_IDENTIFIER"].to_s.strip
app_identifier([app_id]) unless app_id.empty?